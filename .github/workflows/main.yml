
https://bee.beena.space/data/file2/6G5dxq9RxQorm4TrI~V9f23Ljmz5teRRusZqP0rkSz~1muRJnWS3LcqaELkq+RfkuCbYF7bXVal4RR7FWuRjsoQmJ97lUBMISPrnVVVgJ6CgMwNhi8zJ4REN5HzyEhAFOffCGvD7DvuLut55sNxoGIse+BgR4nrEND+aWuF3xkc=/MTA4MA==/aW5kZXgubTN1OA==.m3u8 ?headers=%7B%22origin%22%3A%22https%3A%2F%2Fvideostr.net%22%2C%22referer%22%3A%22https%3A%2F%2Fvideostr.net%2F%22%7D&host=https%3A%2F%2Fnightbreeze17.site

ئەم هەڵەیە ئەدات 
Run # بەکارهێنانی User-Agent و Referer بۆ فێڵکردن لە سێرڤەری پاراستن 

[generic] Extracting URL: https://bee.beena.space/data/file2/6G5dxq9RxQorm4TrI~V9f23Ljmz5teRRusZqP0rkSz~1muRJnWS3LcqaELkq+R...W5kZXgubTN1OA==.m3u8 

[generic] aW5kZXgubTN1OA==: Downloading webpage 

ERROR: [generic] Got HTTP Error 403 caused by Cloudflare anti-bot challenge; see https://github.com/yt-dlp/yt-dlp#impersonation for how to install the required impersonation dependency, and try again with --extractor-args "generic:impersonate" 

Error: Process completed with exit code 1.



ئەمەش کۆدەکەیە بەڵام شێوازی کارکردنی مەگۆڕە بۆ سایتەکانی تر چونکە لەهەنێک شوێنی تر ڤیدۆی پێی دا ئەرم وابکە ئەم جۆرەش دابەزێنی 
name: Direct_To_HuggingFace_CDN
on:
  workflow_dispatch:
    inputs:
      video_url:
        description: 'لینکی ڤیدیۆکە (بە تۆکنەکەیەوە)'
        required: true
      file_name:
        description: 'ناوی فایلەکە لەناو Hugging Face (بە .mp4 کۆتایی بێت)'
        required: true
        default: 'video.mp4'
      referer_url:
        description: 'سەرچاوەی لینکەکە (بۆ تێپەڕاندنی 403 Forbidden)'
        required: true
        default: 'https://zedflix.tv/'

jobs:
  upload_job:
    runs-on: ubuntu-latest
    steps:
      - name: 🏗️ Setup Tools
        run: |
          sudo apt-get update -qq
          # دابەزاندنی ffmpeg زۆر گرنگە بۆ تێکەڵکردنی پارچە ڤیدیۆکانی CDN
          sudo apt-get install -y ffmpeg -qq
          # دابەزاندنی yt-dlp
          python -m pip install -U pip yt-dlp huggingface_hub

      - name: ⬇️ Download Video (Bypass 403 Forbidden)
        run: |
          # بەکارهێنانی User-Agent و Referer بۆ فێڵکردن لە سێرڤەری پاراستن
          yt-dlp \
            --user-agent "Mozilla/5.0 (Windows NT 10.0; Win64; x64) AppleWebKit/537.36 (KHTML, like Gecko) Chrome/122.0.0.0 Safari/537.36" \
            --add-header "Referer: ${{ github.event.inputs.referer_url }}" \
            --add-header "Origin: ${{ github.event.inputs.referer_url }}" \
            -o "video.mp4" "${{ github.event.inputs.video_url }}"

      - name: 🚀 Upload to Hugging Face
        env:
          HF_TOKEN: ${{ secrets.HF_TOKEN }}
        run: |
          python3 -c "
          import os
          from huggingface_hub import HfApi
          
          # وەرگرتنی تۆکن لە سێتیتنگس
          token = os.getenv('HF_TOKEN')
          if not token:
              print('❌ Error: HF_TOKEN is missing in GitHub Secrets!')
              exit(1)
              
          api = HfApi()
          repo_id = 'Sarkoakram/sarko761'
          file_name = '${{ github.event.inputs.file_name }}'
          
          print(f'📤 Sending to Hugging Face: {file_name}...')
          
          api.upload_file(
              path_or_fileobj='video.mp4',
              path_in_repo=file_name,
              repo_id=repo_id,
              repo_type='dataset',
              token=token
          )
          print('✅ Done! Check your Hugging Face repo.')
          "
